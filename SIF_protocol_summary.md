# SIF 协议说明

## 概述

SIF (Single Inline Frame) 单线通信协议，用于电机控制器、智能中控与仪表/报警器的双向通信。

## 物理层

- **线数**: 1 线（原仪表显示线 / 报警器锁车线复用）
- **空闲电平**: 高
- **时钟基准**: Tosc（自适应，常见 32Tosc = 500µs 或 1000µs）
- **容差**: 发送端脉宽 ±20%

---

## 帧结构

```
                        SIF 帧
┌──────────────────────────────────────────────────────┐
│  Sync Low    │ Sync High  │  Data Bytes ...  │  End  │
│  ≥ 8 ms      │ ~32 Tosc   │  每字节 8 bit    │ ≥2ms  │
└──────────────────────────────────────────────────────┘
```

### 同步阶段
1. **Sync Low**: 长低电平 ≥ 8ms（≥ ~992 Tosc），标记帧开始
2. **Sync High**: 短高脉冲 ≈ 32 Tosc，用于测量 Tosc 时钟

### 数据阶段

每 bit 由**一对脉冲**（低→高）组成：

```
Bit 1:  ──┐   ┌──────    (低短 + 高长)
           │   │
           └───┘

Bit 0:  ──┐      ┌──     (低长 + 高短)
           │      │
           └──────┘
```

| Bit | 低脉冲 | 高脉冲 | 判断规则 |
|-----|--------|--------|----------|
| 1 | 短 (~32Tosc) | 长 (~64Tosc) | 高 > 低 |
| 0 | 长 (~64Tosc) | 短 (~32Tosc) | 低 > 高 |

- 8 bit 组成 1 byte，高位在前 (MSB first)
- 每个 bit 以**低脉冲开头**，所以首脉冲永远是低

### 帧结束

数据结束后，低电平保持 ≥ 2ms，表示帧结束。分析器回到 SEEK_SYNC 状态。

### 帧中同步

数据中可能出现新的长低电平 (≥8ms)，分析器会重新同步并开始新帧。

---

## TAILG 应用层 (可选)

在 Raw 字节之上，TAILG 模式解析 TLV 结构：

```
 TAILG 包 (在 SIF 数据段内)
┌────┬────┬─────────┬────────────────────────┬─────┐
│ D0 │ 01 │ TotalLen│  TLV 条目 ...           │ CRC │
│ 1B │ 1B │  1B     │  TotalLen 字节          │ 1B  │
└────┴────┴─────────┴────────────────────────┴─────┘
```

### 字段说明

| 位置 | 字段 | 说明 |
|------|------|------|
| 0 | D0 | 帧头标识 |
| 1 | 01 | 版本/子类型 |
| 2 | TotalLen | TLV 数据总长度 (不含头尾) |
| 3 ~ 2+TotalLen | TLV | 若干 TAG+LEN+Value 条目 |
| 3+TotalLen | CRC | SUM 校验 (D0 起累加，取低 8 位) |

**包总长 = 4 + TotalLen**

### TLV 条目格式

```
┌─────┬─────┬──────────────────┐
│ TAG │ LEN │ Value (LEN 字节) │
│ 1B  │ 1B  │ 变长             │
└─────┴─────┴──────────────────┘
```

- TAG: 1 字节类型标识
- LEN: 1 字节，后续 Value 的字节数 (0 表示无 Value)
- Value: LEN 字节数据，分析器自动跳过不做解析

### SUMCRC 校验

$$\text{SUM} = \sum_{i=0}^{\text{TotalLen}+2} \text{byte}[i] \quad (\text{D0 到 CRC 前一字节})$$

$$\text{CRC} = \text{SUM} \bmod 256$$

- 正确: 气泡显示 `SUMCRC 0xNN`
- 错误: 气泡显示 `CRCERR 0xNN(exp 0xYY)`

---

## 时序参数参考

| 参数 | 32Tosc=500µs | 32Tosc=1000µs | 容差 |
|------|-------------|---------------|------|
| 短脉 | ~500 µs | ~1000 µs | ±20% |
| 长脉 | ~1000 µs | ~2000 µs | ±20% |
| Sync Low | ≥ 8 ms | ≥ 8 ms | — |
| Sync High | ~500 µs | ~1000 µs | — |
| 帧结束 | ≥ 2 ms | ≥ 2 ms | — |
| 毛刺 | < 80 µs | < 80 µs | 忽略 |

---

## 分析器实现参考

- **状态机**: SEEK_SYNC → SYNC_H → DATA
- **脉冲配对**: 低脉冲暂存 → 高脉冲到达后比较位值
- **Tosc 测量**: Sync High 脉宽 / 32
- **SUM 累加**: 从第一个 TAILG 字节 (D0) 开始，到 CRC 前一个字节结束
- **源码**: `SIF_CPP/SIFAnalyzer/SIFAnalyzer.cpp`
