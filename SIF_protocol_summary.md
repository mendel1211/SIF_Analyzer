# SIF 协议说明（摘要）

## 概述
- 采用国际标准 SIF（Single Inline Frame）单线通信协议，用于电机控制器、智能中控与仪表/报警器的双向通信。

## 物理层与接口
- 单线（原仪表显示线／报警器锁车线复用）。
- 采样/计时基准以 Tosc 为单位（32Tosc、64Tosc 等）。

## 传输速率与自适应
- 速率自适应，常见基准：32Tosc = 500 µs（默认）或 32Tosc = 1000 µs。
- 发送端脉宽允许误差 ±20%。

## 同步/帧起始
- 存在较长的同步信号（旧时序示意 >31 ms；新时序以 >992Tosc 的长脉作为同步边界）。
- 同步之后会有一个短间隔（>32Tosc）用于速率确认，然后进入数据位传输。

## 比特编码（时序型）
- 每个数据位由两段脉冲（高/低电平持续时间）构成：
  - Data(1): 短脉（约 32Tosc）后跟 长脉（约 64Tosc）
  - Data(0): 长脉（约 64Tosc）后跟 短脉（约 32Tosc）
- 举例（32Tosc=500 µs 情况）：
  - 短脉宽 ≈ 500 µs（允许 500 ±100 µs）
  - 长脉宽 ≈ 1000 µs（允许 1000 ±200 µs）
- 若 32Tosc=1000 µs，则短≈1000±200 µs，长≈2000±400 µs。

## 判位规则（稳健判定）
1. 若检测到低电平持续时间 > 1.2 × 高电平持续时间，则判为逻辑“0”。
2. 若检测到高电平持续时间 > 1.2 × 低电平持续时间，则判为逻辑“1”。
3. 不应仅以单一高/低持续时间绝对值作为判断标准（需比较两段比值）。

## 误差与建议
- 发送端遵循 ±20% 脉宽容差以保证接收端稳定识别。
- 接收端应以比值（1.2 倍）判定位值，并在同步阶段测量 Tosc 以自适应速率。

---
*该文档基于您提供的协议图片内容摘要，若需加入示例帧、CRC 或包结构，请提供相应细节，我会补充。*
